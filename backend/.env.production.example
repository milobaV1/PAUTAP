DB_USERNAME=training_user
DB_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD
DB_HOST=db
DB_PORT=5432
DB_NAME=pautap

# To generate a strong password, run on server:
# openssl rand -base64 32

PORT=3000
NODE_ENV=production

APP_URL=http://YOUR_SERVER_IP
FRONTEND_URL=http://YOUR_SERVER_IP

JWT_SECRET=CHANGE_THIS_TO_STRONG_SECRET

# To generate a strong JWT secret, run on server:
# openssl rand -base64 64

# ==========================================
# ADMIN SEED DATA
# ==========================================
SEED_ADMIN_EMAIL=admin@pau.edu.ng
SEED_ADMIN_PASSWORD=CHANGE_THIS_ADMIN_PASSWORD

# ==========================================
# EMAIL CONFIGURATION
# ==========================================
# Gmail example (use App Password, not regular password):
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USERNAME=your-email@gmail.com
# EMAIL_PASSWORD=your-app-password
# MAIL_FROM=PAU Training <noreply@pau.edu.ng>

# Outlook/Office365 example:
# EMAIL_HOST=smtp.office365.com
# EMAIL_PORT=587
# EMAIL_USERNAME=your-email@pau.edu.ng
# EMAIL_PASSWORD=your-password
# MAIL_FROM=PAU Training <noreply@pau.edu.ng>

# For now (without email configured):
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USERNAME=
EMAIL_PASSWORD=
MAIL_FROM=

# ==========================================
# REDIS CONFIGURATION
# ==========================================
REDIS_HOST=redis
REDIS_PORT=6379

# ==========================================
# CORS & SECURITY
# ==========================================
# Will be set based on APP_URL and FRONTEND_URL
ALLOWED_ORIGINS=${FRONTEND_URL},${APP_URL}